# BAHG Icons

Branded SVG icon library for BAHG projects and vibe coding workflows.

## Structure

- `/icons` = SVG icon files
- `/manifest.json` = machine-readable icon index

## Usage

Use the manifest file to search icons by name, category, and tags.

CDN base:
https://cdn.jsdelivr.net/gh/batrips/bahg-icons@main/
